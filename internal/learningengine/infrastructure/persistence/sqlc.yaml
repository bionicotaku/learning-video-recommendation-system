version: "2"
sql:
  - engine: "postgresql"
    queries:
      - "query/*.sql"
    schema:
      - "../migration/*.up.sql"
      - "schema/*.sql"
    gen:
      go:
        package: "learningenginesqlc"
        out: "sqlcgen"
        sql_package: "pgx/v5"
        emit_interface: true
        emit_json_tags: true
        emit_empty_slices: true
