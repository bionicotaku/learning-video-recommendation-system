# User migrations

Owns the `app_user` schema:

- `app_user.user_profiles`
- `app_user.user_activity_stats`
- `app_user.user_daily_activity_stats`
- `app_user.feedback_submissions`
- `app_user.feedback_images`

Tracking table: `user_schema_migrations`.
